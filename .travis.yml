os: linux
language: python

sudo: true
python: 3.6

services:
  - docker

install:
  - travis/install-nodejs.sh
  - pip install -r travis/requirements.txt

before_script:
  - pip freeze
  - pwd

script:
  - bash travis/conformance-tests.sh
  # disabled, travis VM has only 1 core- bash tests/cwltest.sh
