#!/bin/bash
for fname in "$1" "$2" "$3"
do
    if [[ $fname == *SSU* ]]; then SSU=$fname; fi
    if [[ $fname == *LSU* ]]; then LSU=$fname; fi
    if [[ $fname == *RF00002* ]]; then FIVES=$fname; fi
done

ln "$SSU" "$4"
ln "$LSU" "$5"
ln "$FIVES" "$6"